name: "211"
epics_prefix: "PPGUN"

cameras:
  # Lucid Vision Labs PHX122S-M, 4024x3036 Mono16, at 192.168.157.12.
  # Served by aravis_ioc.py running on the Windows box on the camera subnet
  # (see vpcam/docs/how_to_install_iocs_on_windows.txt for that setup).
  # NOTE: 12.2 Mpx over CA is ~49 MB per frame -- clients need
  # EPICS_CA_MAX_ARRAY_BYTES well above the old 40 MB default.
  - id: "PPGUNCAM"
